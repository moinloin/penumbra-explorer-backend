name: Rust CI/CD Pipeline (Stage)

on:
  pull_request:
    branches:
      - stage
  workflow_dispatch:

env:
  APP_NAME_STAGE: penumbra-explorer
  SERVER_USER_STAGE: deploy-penumbra-explorer-stage
  SERVER_HOST_STAGE: ${{ secrets.SERVER_HOST_STAGE }}

jobs:
  code-quality:
    name: Code Quality
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: actions-rs/toolchain@v1
        with:
          toolchain: 1.83.0
          override: true
          components: rustfmt, clippy

      - name: Cache dependencies
        uses: actions/cache@v3
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Run clippy with pedantic settings
        run: cargo clippy --all-targets --all-features --workspace -- -W clippy::pedantic -D warnings

  test:
    name: Run Tests
    runs-on: ubuntu-latest
    needs: code-quality
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: actions-rs/toolchain@v1
        with:
          toolchain: 1.81.0
          override: true

      - name: Cache dependencies
        uses: actions/cache@v3
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Run tests
        run: cargo test --all

  deploy:
    name: Deploy to Staging Server
    runs-on: ubuntu-latest
    needs: [code-quality, test]
    if: github.event_name == 'pull_request' && github.event.pull_request.base.ref == 'stage'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: false
          tags: ${{ env.APP_NAME_STAGE }}:latest
          outputs: type=docker,dest=/tmp/${{ env.APP_NAME_STAGE }}.tar

      - name: Install SSH key
        uses: shimataro/ssh-key-action@v2
        with:
          key: ${{ secrets.DEPLOY_SSH_KEY_STAGE }}
          known_hosts: unnecessary

      - name: Add server to known hosts
        run: ssh-keyscan -H ${{ env.SERVER_HOST_STAGE }} >> ~/.ssh/known_hosts

      - name: Transfer Docker image
        run: |
          scp /tmp/${{ env.APP_NAME_STAGE }}.tar ${{ env.SERVER_USER_STAGE }}@${{ env.SERVER_HOST_STAGE }}:/home/deploy-penumbra-explorer-stage/deployments/

      - name: Deploy with Blue-Green mechanism
        run: |
          ssh ${{ env.SERVER_USER_STAGE }}@${{ env.SERVER_HOST_STAGE }} '/home/deploy-penumbra-explorer-stage/deployments/deploy.sh'

      - name: Check deployment status
        run: |
          ssh ${{ env.SERVER_USER_STAGE }}@${{ env.SERVER_HOST_STAGE }} 'tail -20 /home/deploy-penumbra-explorer-stage/deployments/deploy.log'